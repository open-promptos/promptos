{
  "name": "promptos-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "PromptOS monorepo - DSL core, abilities, LLM integration and VSCode host.",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "pnpm@9.15.0",
  "workspaces": [
    "packages/*",
    "apps/*",
    "examples/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.6.1",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "@types/node": "^24.10.1",
    "@types/vscode": "^1.90.0" 
  }
}
